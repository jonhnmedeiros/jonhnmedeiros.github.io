version: '3.8'

services:
  dev:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspace:cached
    ports:
      - "5173:5173"
    command: /bin/bash -c "while sleep 1000; do :; done"
    working_dir: /workspace
